let results=document.querySelector("#result")
const API=" https://pokeapi.co/api/v2/pokemon?limit=0&offset=20"

async function fetchpokimon(){
    let response=await fetch(API)
    console.log(response)
    let result=await response.json()
    // console.log(result.results)
    getdata(result.results)
}
fetchpokimon()


async function getdata(arr){
   console.log(arr)
  for(let x of arr){
     

    let fetcheing=await fetch(x.url)
    let resutlfetch=await fetcheing.json()
    console.log(resutlfetch)
    getfecthing(resutlfetch)
  }
}
function getfecthing(arr){
    console.log(arr)

    let divmain=document.createElement("div")
    divmain.classList.add("maindiv")
    divmain.innerHTML=`
    <img src="${arr.sprites.front_shiny}" class="pokimonimg">
    <p>Name:   ${arr.name}  </p>
    <p>type:  ${arr.types[0].type.name}  </p>
    
    `
    results.append(divmain)
}

Height:
Weight:
hp:
attack:
defence:
special_attack:
special_defence:
speed:
